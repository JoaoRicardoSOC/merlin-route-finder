openapi: 3.0.3
info:
  title: Merlin Route Finder API
  description: >
    Contrato da API REST do Merlin Route Finder (FIAP Challenge 2026 / Leroy Merlin).
    Escrito à mão como fonte de verdade API-First (ver docs/planejamento-tecnico.md),
    antes da implementação dos controllers reais. Cobre os recursos das Fases 0 a 3
    do backlog revisado (docs/backlog-trello-revisado.md).
  version: "0.1.0"
servers:
  - url: /api/v1

tags:
  - name: Sessao
    description: Ciclo de vida da sessão do cliente (Totem e Mobile)
  - name: Catalogo
    description: Busca e detalhamento de produtos
  - name: Roteiro
    description: Montagem da lista de compras (Totem) e execução da rota (Mobile)
  - name: Handoff
    description: Transição de dispositivo Totem -> Mobile via QR Code/JWT
  - name: RupturaEstoque
    description: Tratamento de ruptura física e sugestão de substituto por IA
  - name: Chat
    description: Assistente conversacional (RAG)

paths:
  /sessoes:
    post:
      tags: [Sessao]
      summary: Inicializar sessão (UC-001)
      description: Acionado quando o cliente inicia interação no Totem. Cria uma Sessao ACTIVE com uma ListaRoteiro vazia.
      responses:
        "201":
          description: Sessão criada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Sessao"
        "500":
          $ref: "#/components/responses/ErroInterno"

  /sessoes/{sessaoId}:
    get:
      tags: [Sessao]
      summary: Consultar status/ciclo de vida da sessão
      parameters:
        - $ref: "#/components/parameters/SessaoId"
      responses:
        "200":
          description: Sessão encontrada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Sessao"
        "404":
          $ref: "#/components/responses/NaoEncontrado"

  /sessoes/{sessaoId}/concluir:
    post:
      tags: [Sessao]
      summary: Concluir rota e encerrar sessão (UC-014)
      description: Acionado pelo Mobile quando o cliente finaliza a caminhada (manualmente ou após coletar todos os itens). Transiciona StatusSessao para COMPLETED.
      parameters:
        - $ref: "#/components/parameters/SessaoId"
      responses:
        "200":
          description: Sessão encerrada
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Sessao"
        "404":
          $ref: "#/components/responses/NaoEncontrado"
        "409":
          description: Sessão já não está mais ativa
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandardError"

  /produtos:
    get:
      tags: [Catalogo]
      summary: Busca paginada e fuzzy de produtos (UC-002)
      parameters:
        - name: query
          in: query
          required: false
          schema:
            type: string
          description: Termo de busca (tolerante a pequenos erros de digitação)
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          required: false
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Página de resultados
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProdutoPage"

  /produtos/{produtoId}:
    get:
      tags: [Catalogo]
      summary: Detalhamento de produto e resolução síncrona de inventário (UC-003)
      parameters:
        - $ref: "#/components/parameters/ProdutoId"
      responses:
        "200":
          description: Produto encontrado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProdutoDetalhado"
        "404":
          $ref: "#/components/responses/NaoEncontrado"

  /produtos/{produtoId}/estoque:
    patch:
      tags: [Catalogo]
      summary: Simular alteração de estoque (ferramenta interna de demonstração)
      description: >
        Não faz parte de nenhum caso de uso do cliente final. Permite zerar ou restaurar o
        saldoEstoque de um produto sob demanda, para disparar o fluxo de ruptura (UC-013)
        de forma confiável durante gravação de vídeo ou apresentação ao vivo.
      parameters:
        - $ref: "#/components/parameters/ProdutoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EstoqueUpdateRequest"
      responses:
        "200":
          description: Estoque atualizado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Produto"
        "404":
          $ref: "#/components/responses/NaoEncontrado"

  /sessoes/{sessaoId}/roteiro:
    get:
      tags: [Roteiro]
      summary: Consultar a lista de compras da sessão (UC-005)
      parameters:
        - $ref: "#/components/parameters/SessaoId"
      responses:
        "200":
          description: Lista de roteiro da sessão
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListaRoteiroResponse"
        "404":
          $ref: "#/components/responses/NaoEncontrado"
        "409":
          $ref: "#/components/responses/SessaoInativa"

  /sessoes/{sessaoId}/roteiro/itens:
    post:
      tags: [Roteiro]
      summary: Adicionar produto ao roteiro (UC-004)
      description: Sem limite máximo de itens (decisão de negócio de 17/08/2026).
      parameters:
        - $ref: "#/components/parameters/SessaoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdicionarItemRequest"
      responses:
        "201":
          description: Item adicionado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemRoteiroDetalhado"
        "404":
          $ref: "#/components/responses/NaoEncontrado"
        "409":
          $ref: "#/components/responses/SessaoInativa"

  /sessoes/{sessaoId}/roteiro/itens/{itemId}:
    delete:
      tags: [Roteiro]
      summary: Remover produto da lista (UC-006)
      parameters:
        - $ref: "#/components/parameters/SessaoId"
        - $ref: "#/components/parameters/ItemId"
      responses:
        "204":
          description: Item removido
        "404":
          $ref: "#/components/responses/NaoEncontrado"
        "409":
          $ref: "#/components/responses/SessaoInativa"

  /sessoes/{sessaoId}/roteiro/pontos-interesse:
    post:
      tags: [Roteiro]
      summary: Incluir ponto de interesse na rota em andamento (UC-012)
      description: >
        Acionado pelo Mobile para incluir um ponto de apoio (banheiro ou caixa) no trajeto,
        sem que isso seja um produto do roteiro de compras. Localiza o PontoMapa do tipo
        solicitado mais próximo e insere na sequência de navegação sem alterar a ordem dos
        itens de compra já calculada.
      parameters:
        - $ref: "#/components/parameters/SessaoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncluirPontoInteresseRequest"
      responses:
        "200":
          description: Rota atualizada com o ponto de interesse incluído
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RotaCalculada"
        "404":
          $ref: "#/components/responses/NaoEncontrado"

  /roteiro/itens/{itemId}/coletar:
    patch:
      tags: [Roteiro]
      summary: Marcar item como coletado durante a caminhada (UC-014)
      description: >
        Acionado pelo Mobile quando o cliente confirma ter pego um produto. É o mecanismo
        que faz o "ponto" avançar visualmente na simulação de caminhada do Mobile PWA
        (estratégia de demo da banca final).
      parameters:
        - $ref: "#/components/parameters/ItemId"
      responses:
        "200":
          description: Item marcado como coletado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ItemRoteiroDetalhado"
        "404":
          $ref: "#/components/responses/NaoEncontrado"

  /handoff:
    post:
      tags: [Handoff]
      summary: Gerar ou regerar handoff (concluir lista e transicionar para o Mobile) (UC-010)
      description: >
        Valida a sessão, executa o algoritmo de roteamento (Nearest Neighbor com
        refinamento 2-opt), gera um JWT assinado com TTL de 5 minutos e uso único, e
        persiste o token na ListaRoteiro.


        **Regeneração:** chamar de novo para a mesma sessão emite um token novo e
        invalida o anterior. É o caminho de recuperação quando o QR Code expira antes
        de ser escaneado, ou quando o cliente troca de aparelho — a jornada continua de
        onde estava, sem refazer a montagem da lista. Se a caminhada já tiver começado
        (algum item coletado), a ordem das paradas é preservada em vez de recalculada,
        para não embaralhar a navegação em curso.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HandoffRequest"
      responses:
        "201":
          description: Handoff gerado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HandoffResponse"
        "404":
          $ref: "#/components/responses/NaoEncontrado"
        "409":
          description: Lista de roteiro vazia ou sessão inválida
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandardError"

  /handoff/validate:
    post:
      tags: [Handoff]
      summary: Validar token de handoff e iniciar navegação (UC-011)
      description: >
        Acionado quando o Mobile escaneia o QR Code. Valida o JWT, invalida-o
        imediatamente após uso (uso único) e retorna a matriz de rota calculada,
        os itens e as coordenadas.


        **O token vai no corpo, nunca na URL.** Era `GET .../validate?token=` até a
        Fase 3; mudou por duas razões que se somam: URL fica gravada em histórico de
        navegador e em log de servidor, e a operação **consome** o token e renova a
        sessão — efeitos colaterais que um GET nunca deveria ter. Mudança quebrante,
        combinada com a dupla de frontend antes de entrar (ver D-25 e D-44).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValidarHandoffRequest"
      responses:
        "200":
          description: Token válido — rota liberada para navegação
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RotaCalculada"
        "400":
          description: Token ausente ou em branco
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandardError"
        "401":
          description: >
            Token inválido, já utilizado ou expirado. O campo `error` distingue os dois
            desfechos: `Token de Handoff Expirado` significa que basta o Totem gerar um
            QR Code novo (POST /handoff na mesma sessão) e a jornada continua; qualquer
            outro rótulo é `Token de Handoff Invalido`, sem recuperação automática.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandardError"
        "409":
          $ref: "#/components/responses/SessaoInativa"

  /roteiro/itens/{itemId}/ruptura:
    post:
      tags: [RupturaEstoque]
      summary: Tratar ruptura de estoque e sugerir substituto (UC-013)
      description: >
        Acionado quando o cliente sinaliza "Prateleira Vazia" no Mobile. Registra a
        ruptura, faz uma pré-filtragem espacial por corredores adjacentes ao PontoMapa
        do produto em falta, e aciona o Gemini via function calling para eleger o
        substituto semântico mais adequado dentre os candidatos já filtrados
        espacialmente (nunca por texto livre do modelo).
      parameters:
        - $ref: "#/components/parameters/ItemId"
      responses:
        "200":
          description: Substituto sugerido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RupturaEstoqueResponse"
        "404":
          $ref: "#/components/responses/NaoEncontrado"
        "409":
          $ref: "#/components/responses/SessaoInativa"
        "422":
          description: >
            Ruptura registrada, mas nenhum substituto plausível encontrado nas
            proximidades - seja porque nenhum produto disponível está dentro do raio,
            seja porque o assistente avaliou os candidatos e concluiu que nenhum cumpre
            a mesma função.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StandardError"

  /sessoes/{sessaoId}/chat/mensagens:
    get:
      tags: [Chat]
      summary: Consultar histórico conversacional da sessão (UC-007 a UC-009)
      parameters:
        - $ref: "#/components/parameters/SessaoId"
      responses:
        "200":
          description: >
            Histórico da conversa em ordem cronológica, possivelmente vazio. Sessão
            encerrada não impede a leitura: o histórico é registro do que já aconteceu.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatMensagem"
        "404":
          $ref: "#/components/responses/NaoEncontrado"
    post:
      tags: [Chat]
      summary: Enviar mensagem ao assistente virtual (UC-007 a UC-009)
      description: >
        O assistente consulta o catálogo real da loja por function calling antes de
        responder, e tem escopo fechado: recusa assuntos fora de reforma, construção,
        bricolagem, decoração e jardinagem. Ele sugere produtos, mas nunca altera o
        roteiro - adicionar continua sendo ação do cliente (UC-004).
      parameters:
        - $ref: "#/components/parameters/SessaoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EnviarMensagemRequest"
      responses:
        "201":
          description: >
            Mensagem do assistente. Se o assistente estiver indisponível, a resposta é uma
            mensagem honesta de indisponibilidade, que deliberadamente não entra no
            histórico - a pergunta do cliente fica salva sem resposta, que é o que de fato
            aconteceu.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatMensagem"
        "404":
          $ref: "#/components/responses/NaoEncontrado"
        "409":
          $ref: "#/components/responses/SessaoInativa"

components:
  parameters:
    SessaoId:
      name: sessaoId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ProdutoId:
      name: produtoId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ItemId:
      name: itemId
      in: path
      required: true
      schema:
        type: string
        format: uuid

  responses:
    NaoEncontrado:
      description: Recurso não encontrado
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/StandardError"
    SessaoInativa:
      description: >
        A sessão existe mas não está mais ativa (encerrada, expirada ou abandonada), então a
        operação não é permitida. Diferente do 404: o recurso existe, o estado é que impede.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/StandardError"
    ErroInterno:
      description: Erro interno inesperado
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/StandardError"

  schemas:
    StatusSessao:
      type: string
      enum: [ACTIVE, COMPLETED, EXPIRED, ABANDONED]
    TipoPonto:
      type: string
      enum: [PRATELEIRA, BANHEIRO, CAIXA, TOTEM]
    Remetente:
      type: string
      enum: [USER, ASSISTANT]

    Sessao:
      type: object
      properties:
        id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/StatusSessao"
        criadoEm:
          type: string
          format: date-time
        expiracaoTtl:
          type: string
          format: date-time

    PontoMapa:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tipo:
          $ref: "#/components/schemas/TipoPonto"
        corredor:
          type: string
        coordenadaX:
          type: integer
        coordenadaY:
          type: integer

    Produto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        sku:
          type: string
        nome:
          type: string
        preco:
          type: number
          format: double
        saldoEstoque:
          type: integer
        pontoMapaId:
          type: string
          format: uuid

    ProdutoDetalhado:
      allOf:
        - $ref: "#/components/schemas/Produto"
        - type: object
          properties:
            pontoMapa:
              $ref: "#/components/schemas/PontoMapa"

    ProdutoPage:
      type: object
      properties:
        content:
          type: array
          items:
            $ref: "#/components/schemas/Produto"
        page:
          type: integer
        size:
          type: integer
        totalElements:
          type: integer
        totalPages:
          type: integer

    ItemRoteiro:
      type: object
      properties:
        id:
          type: string
          format: uuid
        produtoId:
          type: string
          format: uuid
        ordemCaminho:
          type: integer
        coletado:
          type: boolean

    ItemRoteiroDetalhado:
      allOf:
        - $ref: "#/components/schemas/ItemRoteiro"
        - type: object
          properties:
            produto:
              $ref: "#/components/schemas/Produto"

    ListaRoteiroResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        sessaoId:
          type: string
          format: uuid
        quantidadeItens:
          type: integer
        itens:
          type: array
          items:
            $ref: "#/components/schemas/ItemRoteiroDetalhado"

    AdicionarItemRequest:
      type: object
      required: [produtoId]
      properties:
        produtoId:
          type: string
          format: uuid

    IncluirPontoInteresseRequest:
      type: object
      required: [tipo]
      properties:
        tipo:
          type: string
          enum: [BANHEIRO, CAIXA]
          description: Tipo de ponto de apoio a incluir na rota (não é um item de compra)

    EstoqueUpdateRequest:
      type: object
      required: [saldoEstoque]
      properties:
        saldoEstoque:
          type: integer
          minimum: 0

    ValidarHandoffRequest:
      type: object
      required: [token]
      properties:
        token:
          type: string
          description: JWT lido do QR Code. Vai no corpo para não ficar gravado em URL.

    HandoffRequest:
      type: object
      required: [sessaoId]
      properties:
        sessaoId:
          type: string
          format: uuid

    HandoffResponse:
      type: object
      properties:
        handoffUrl:
          type: string
          description: URL a ser codificada no QR Code exibido no Totem
        token:
          type: string
        tokenExpiracao:
          type: string
          format: date-time

    PontoRota:
      type: object
      description: Um ponto na sequência de navegação calculada (item de compra ou ponto de interesse)
      properties:
        ordem:
          type: integer
        item:
          $ref: "#/components/schemas/ItemRoteiroDetalhado"
        pontoMapa:
          $ref: "#/components/schemas/PontoMapa"

    RotaCalculada:
      type: object
      properties:
        sessaoId:
          type: string
          format: uuid
        listaRoteiroId:
          type: string
          format: uuid
        pontos:
          type: array
          items:
            $ref: "#/components/schemas/PontoRota"

    RupturaEstoqueResponse:
      type: object
      properties:
        produtoOriginalId:
          type: string
          format: uuid
        produtoSugeridoId:
          type: string
          format: uuid
        produtoSugerido:
          $ref: "#/components/schemas/ProdutoDetalhado"
        justificativa:
          type: string
          description: Explicação em linguagem natural para a sugestão
        origemSugestao:
          type: string
          description: >
            Quem elegeu o substituto. ASSISTENTE_IA quando o modelo escolheu entre os
            candidatos pré-filtrados; PROXIMIDADE quando o assistente estava indisponível
            e o sistema caiu para o produto disponível mais próximo, sem análise semântica.
            O frontend deve usar este campo para não rotular como recomendação inteligente
            o que foi apenas o item mais perto.
          enum: [ASSISTENTE_IA, PROXIMIDADE]

    ChatMensagem:
      type: object
      properties:
        id:
          type: string
          format: uuid
        sessaoId:
          type: string
          format: uuid
        remetente:
          $ref: "#/components/schemas/Remetente"
        conteudo:
          type: string
        enviadoEm:
          type: string
          format: date-time

    EnviarMensagemRequest:
      type: object
      required: [conteudo]
      properties:
        conteudo:
          type: string

    StandardError:
      type: object
      description: Espelha br.com.jence.backend.presentation.response.StandardError
      properties:
        timestamp:
          type: string
          format: date-time
        status:
          type: integer
        error:
          type: string
        message:
          type: string
        path:
          type: string
        validationErrors:
          type: array
          nullable: true
          items:
            $ref: "#/components/schemas/ValidationError"

    ValidationError:
      type: object
      properties:
        field:
          type: string
        message:
          type: string
