<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>br.com.jence</groupId>
	<artifactId>backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<!-- Sobrescrito para vazio pelo profile "integracao". -->
		<excluded.test.groups>integracao</excluded.test.groups>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>com.oracle.database.jdbc</groupId>
			<artifactId>ojdbc11</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
    		<groupId>org.springdoc</groupId>
    		<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    		<version>3.1.0</version>
		</dependency>
	</dependencies>

	<profiles>
		<!--
			Testes marcados com @Tag("integracao") sobem o contexto contra o Oracle e exigem
			credenciais. Ficam de fora da execucao padrao para que `mvnw test` passe em
			qualquer maquina; rodar com `mvnw test -Pintegracao` quando houver banco.
		-->
		<profile>
			<id>integracao</id>
			<properties>
				<excluded.test.groups></excluded.test.groups>
			</properties>
		</profile>

		<!--
			O AVISO DO QUE NAO RODOU. Ver O-36.

			A exclusao acima e uma boa decisao, mas ela e SILENCIOSA: quem roda `mvnw test`
			ve "BUILD SUCCESS" e conclui que a suite passou inteira. Nao passou - fica de
			fora a maior parte dela, justamente a que toca o banco.

			Isso ja custou caro uma vez. O commit da D-85 foi dado como verificado com a
			suite padrao; rodando o perfil completo, oito testes falhavam, incluindo o que
			garante que o substituto oferecido durante a banca faz sentido.

			O aviso nao cita quantidade de proposito. Numero fixo em mensagem envelhece
			sozinho: o proprio texto da O-36 dizia "19 testes de integracao" quando ja eram
			132, e ninguem percebeu porque o numero nao tem quem o atualize.

			Por que um profile com activeByDefault, e nao um plugin no build principal: o
			Maven DESATIVA os profiles activeByDefault assim que qualquer profile e ativado
			na linha de comando. Entao `mvnw test` avisa, e `mvnw test -Pintegracao` nao
			avisa - que e exatamente o comportamento desejado, sem nenhuma condicao escrita
			a mao.
		-->
		<profile>
			<id>avisar-testes-de-fora</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>3.1.0</version>
						<executions>
							<execution>
								<id>avisar-integracao</id>
								<phase>test</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<echo level="warning">
========================================================================
 ATENCAO: esta execucao NAO rodou os testes de integracao.

 Os testes marcados com @Tag("integracao") sobem o contexto contra o
 Oracle e ficam de fora por padrao. Sao quase metade da suite.

 "BUILD SUCCESS" aqui NAO significa que a suite inteira passa.

 Para rodar tudo, com as credenciais no ambiente:

     mvnw test -Pintegracao

 Obrigatorio antes de qualquer commit que mexa na massa de dados.
 Ver O-36 em docs/observacoes.md.
========================================================================
										</echo>
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<plugins>
			<!--
			  Cobertura de testes. A suite PADRAO tem 150 testes e ninguem sabia que fracao do codigo
			  eles tocam - 19 casos de uso em producao contra 5 testes unitarios de caso de uso,
			  com o resto vindo dos testes de integracao. Sem medida, "temos testes" e uma
			  afirmacao sem tamanho.

			  Nao ha limite minimo configurado de proposito: por ora o numero serve para
			  encontrar o que ninguem exercita, nao para reprovar a construcao.
			-->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.13</version>
				<executions>
					<execution>
						<id>preparar-agente</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>relatorio</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<excludedGroups>${excluded.test.groups}</excludedGroups>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
